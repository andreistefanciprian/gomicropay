apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql-ledger
  labels:
    app: mysql-ledger
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mysql-ledger
  template:
    metadata:
      labels:
        app: mysql-ledger
    spec:
      containers:
        - name: mysql-ledger
          image: mysql:9.4@sha256:135bc87cce147c3d28cecb9ad270b814cb52805af7ddeea83bfcaf157d05a6b2
          envFrom:
            - secretRef:
                name: mysql-ledger-secret
            - configMapRef:
                name: mysql-ledger-configmap
          volumeMounts:
            - name: init-sql
              mountPath: /docker-entrypoint-initdb.d
          securityContext:
            privileged: true
          ports:
            - containerPort: 3306
      volumes:
        - name: init-sql
          configMap:
            name: mysql-ledger-init-sql
            items:
              - key: init.sql
                path: init.sql