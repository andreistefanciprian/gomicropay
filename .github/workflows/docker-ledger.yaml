name: ledger container image build and push to DockerHub

on:
  push:
    tags:
      - '*'
    branches:
      - 'main'
    paths:
      - 'ledger/go.*'
      - 'ledger/**/*.go'
      - 'ledger/infra/Dockerfile'
      - '.github/workflows/docker-ledger.yaml'
  pull_request:
    branches: [ 'main' ]
    paths:
      - 'ledger/go.*'
      - 'ledger/**/*.go'
      - 'ledger/infra/Dockerfile'
      - '.github/workflows/docker-ledger.yaml'
  workflow_dispatch:
  
permissions:
  contents: read
  id-token: write
  attestations: write
  
jobs:

  generate-build-id:
    runs-on: ubuntu-latest
    outputs:
      BUILD_ID: ${{ steps.prep.outputs.BUILD_ID }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Generate build ID
        id: prep
        run: |
          branch=${GITHUB_REF##*/}
          sha=${GITHUB_SHA::8}
          ts=$(date +%s)
          echo "BUILD_ID=${branch}-${sha}-${ts}" >> $GITHUB_OUTPUT

  push-docker-image:
    needs: [generate-build-id]
    uses: andreistefanciprian/gh-reusable-workflows/.github/workflows/build_and_push_docker_image_to_dockerhub.yaml@main
    with:
      image_name: andreistefanciprian/gomicropay-ledger
      image_tag: ${{ needs.generate-build-id.outputs.BUILD_ID }}
      dockerhub_username: andreistefanciprian
      file: ledger/infra/Dockerfile
      context: ./ledger
    secrets: inherit