apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql-payments
  labels:
    app: mysql-payments
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mysql-payments
  template:
    metadata:
      labels:
        app: mysql-payments
    spec:
      containers:
        - name: mysql-payments
          image: mysql:9.5@sha256:57c892ddeb17e6c7dddf219914db017b2cf3e2797806a75424444b83622cc8ef
          envFrom:
            - secretRef:
                name: mysql-payments-secret
            - configMapRef:
                name: mysql-payments-configmap
          volumeMounts:
            - name: init-sql
              mountPath: /docker-entrypoint-initdb.d
          securityContext:
            privileged: true
          ports:
            - containerPort: 3306
      volumes:
        - name: init-sql
          configMap:
            name: mysql-payments-init-sql
            items:
              - key: init.sql
                path: init.sql